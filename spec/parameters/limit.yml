name: limit
in: path
description: |
  Number of elements to be returned.
  The limit should be multiple of "db.pageSizeMin" and not higher than "db.pageSizeMax".
  The settings are adjustable via a configuration file (rest/resources/rest.json) per REST instance.
required: true
schema:
  type: integer
  format: int32
