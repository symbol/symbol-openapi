type: object
required:
  - apiNode
  - db
properties:
  apiNode:
    type: string
    enum:
      - up
      - down
    description: apiNode service status.
  db:
    type: string
    enum:
      - up
      - down
    description: Database service status.