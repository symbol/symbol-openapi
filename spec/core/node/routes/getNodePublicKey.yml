tags:
  - Node routes
summary: Get node's public key.
description: Returns trusted (dual nodes) peeer node's public key which will used in NodeKeyLink transaction.
operationId: getNodePublicKey
responses:
  "200":
    description: success
    content:
      application/json:
        schema:
          $ref: "../schemas/NodePublicKeyDTO.yml"