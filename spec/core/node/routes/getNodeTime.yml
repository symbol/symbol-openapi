tags:
  - Node routes
summary: Get the node time
description: Gets the node time for the moment the request was recieved and the reply was sent. Response timestamps represent milliseconds since the genesis block.
operationId: getNodeTime
responses:
  "200":
    description: success
    content:
      application/json:
        schema:
          $ref: "../schemas/NodeTimeDTO.yml"
