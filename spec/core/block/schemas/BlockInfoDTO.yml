type: object
required:
  - meta
  - block
properties:
  id:
    type: string
    description: Block identifier within the node's database
  meta:
    $ref: "./BlockMetaDTO.yml"
  block:
    $ref: "./BlockDTO.yml"
