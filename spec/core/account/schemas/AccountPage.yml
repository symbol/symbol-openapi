type: object
required:
  - data
properties:
  data:
    type: array
    description: Array of accounts.
    items:
      $ref: "./AccountInfoDTO.yml"
  pagination:
    $ref: "../../../schemas/Pagination.yml"
