tags:
  - Account routes
summary: Get beneficiary blocks
description: Gets an array of blocks where the account is the beneficiary.
operationId: getBeneficiaryBlocks
parameters:
  - $ref: "../../../parameters/pageSize.yml"
  - $ref: "../../../parameters/id.yml"
  - $ref: "../../../parameters/ordering.yml"
responses:
  "200":
    description: success
    content:
      application/json:
        schema:
          type: array
          description: Array of blocks information.
          items:
            $ref: "../../block/schemas/BlockInfoDTO.yml"
  "409":
    $ref: "../../../responses/InvalidArgument.yml"
