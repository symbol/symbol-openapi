type: object
description: Merkle tree branch link.
required:
  - bit
  - link
properties:
  bit:
    description: Branch link nibble bit index (hexadecimal).
    type: string
  link:
    description: Branch link hash (64-character hex).
    $ref: "./Hash256.yml"
    