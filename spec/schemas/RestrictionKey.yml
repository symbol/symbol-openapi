type: string
description: |
  Arbitrary 64-bit numeric identifier of a mosaic restriction, defined by the creator and scoped to a mosaic.
  It can be derived from a human-readable name by hashing it, and is represented as a decimal string in stored restriction entries.
examples:
  - "790526"
